[gcode_macro PRINT_START_HEATSOAK]
gcode:
  {% set PRINT_START_SETTINGS = printer['gcode_macro _PRINT_START_SETTINGS'] %}
  {% set target_bed = PRINT_START_SETTINGS.target_bed %}
  {% set target_chamber = PRINT_START_SETTINGS.target_chamber %}
  {% set target_chamber_minimal = PRINT_START_SETTINGS.target_chamber_minimal %}
  {% set target_chamber_wait = PRINT_START_SETTINGS_target_chamber_wait %}
  {% set x_wait = PRINTER_START_SETTINGS.x_wait %}
  {% set y_wait = PRINTER_START_SETTINGS.y_wait %}

  
  # Check if the bed temp is higher than 90c - if so then trigger a heatsoak.
  {% if target_bed|int > 90 %}
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"                                                          # Display info on display

    G1 X{x_wait} Y{y_wait} Z50 F9000                                                                   # Go to center of the bed
    M190 S{target_bed}                                                                                 # Heat the  bed to target temp
    #SET_DISPLAY_TEXT MSG="Heatsoak: {target_chamber_wait}c"                                            # Display info on display
    TEMPERATURE_WAIT SENSOR="temperature_sensor chamber" MINIMUM={target_chamber_wait}                 # Waits for chamber temp

  # If the bed temp is not over 90c, then skip the heatsoak and just heat up to set temp with a 5 min soak
  {% else %}
    #SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"                                                          # Display info on display
    G1 X{x_wait} Y{y_wait} Z15 F9000                                                                   # Go to center of the bed
    M190 S{target_bed}                                                                                 # Heat the  bed to target temp
    #SET_DISPLAY_TEXT MSG="Soak for 5 min"                                                               # Display info on display
    G4 P300000                                                                                         # Wait 5 min for the bedtemp to stabilize
  {% endif %}

  # Preheat hotend
  #SET_DISPLAY_TEXT MSG="Hotend: 150c"                                                                  # Display info on display
  M109 S150                                                                                            # Heat the extruder to 150c